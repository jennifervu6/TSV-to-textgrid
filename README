Generate TextGrid from TSV

Usage:

python generate_textgrid.py input.tsv [output.TextGrid] [options]

Options: 
--mode {auto,point,interval} Choose tier type (default auto) 
--duration FLOAT Set overall xmax in seconds 
--tier-name NAME Tier name (default: events) 
--delimiter DELIM Input delimiter (default tab) 
--time-col INT 0-based time column (default 0) 
--label-col INT 0-based label column (default 1) 
--tail FLOAT Added to last timestamp if duration not set (default 1.0)

Examples: python generate_textgrid.py times.tsv python generate_textgrid.py times.tsv out.TextGrid --mode point python generate_textgrid.py times.tsv --delimiter "," --time-col 0 --label-col 2

Notes:

Input file should contain numeric seconds in the specified time column.
Lines starting with # are ignored.
